<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.ac.hansung.demap">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".DeMapApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddPlaceFormActivity"></activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".NaverSearchContentActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.main.MainActivity" />
        </activity>
        <activity
            android:name=".SearchNaverActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CreateFolderActivity" />

        <activity android:name=".ui.login.LoginActivity">
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>

        <activity android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.hotPlace.HotPlaceActivity">
        </activity>
        <activity android:name=".ui.nickname.NickNameActivity" />
        <activity android:name=".ui.signup.SignupActivity" />
        <activity android:name=".FolderListActivity" />
        <activity android:name=".MyfolderViewActivity" />
        <activity android:name=".FolderContentActivity" />
        <activity android:name=".FolderContentEditorActivity"/>
        <activity android:name=".NoticeActivity"/>
        <activity android:name=".AddPlaceToFolderActivity" /> <!-- 네이버 api -->
        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="3vtiw45s9x" />
    </application>

</manifest>